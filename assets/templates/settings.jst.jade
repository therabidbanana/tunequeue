form
  #guest_list
    .guest
      input(name="guest_list[]", placeholder="@twitter_username")
      a.remove x
    // textarea(name="guest_list", id="guest_list", placeholder="guest list, one member per line...")
  input(type="submit")
